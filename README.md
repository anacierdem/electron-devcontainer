# Electron devcontainer setup

Very basic devcontainer setup for running Electorn apps. Supports Linux & MacOS.

Run `npm start` in container. Requires XQuartz on MacOS and doesn't support 3D acceleration.

Add `--ozone-platform=wayland` as electron flags if running on Wayland.